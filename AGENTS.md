# RetroLens 仓库协作规则

本文件作用域覆盖仓库根目录及其所有子目录。

## 必须遵守

1. 每次对项目代码进行任何级别的改动（大改或小改）时，必须同步更新项目版本号。
2. 每次改动后，必须同步检查并更新以下文档，确保内容与当前代码一致：
   - `README.md`
   - `docs/FRONTEND.md`
   - `docs/BACKEND.md`
   - `docs/DEPLOYMENT.md`
   - `docs/USER_MANUAL.md`
   - `CHANGELOG.md`
3. 如果项目当前没有真实后端，也必须维护 `docs/BACKEND.md`，明确写明“当前为前端/小程序独立项目，无独立后端服务”。
4. 提交前必须至少完成一次与本次改动相关的构建或验证，并在最终说明中写出验证结果。

## 版本维护约定

1. 常规功能、体验、文档同步更新，默认提升到下一个补丁或小版本。
2. 用户可直接指定版本号；若用户指定，则以用户要求为准。
3. 需要同步检查的常见版本位置包括但不限于：
   - `package.json`
   - `src/manifest.json`
   - 页面/设置中的版本展示文本
   - README 与更新日志中的版本说明

## 文档维护约定

1. README 负责项目总览、特性、快速开始、文档索引。
2. 前端文档负责页面、组件、状态管理、交互与分享能力说明。
3. 部署文档负责本地开发、构建、微信开发者工具导入与发布流程。
4. 使用手册面向最终使用者，描述拍摄、特效、比例、分享、暗房、设置等操作。
5. 更新日志按版本记录新增、优化、修复与文档变更。
